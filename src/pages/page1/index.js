// chinese.js
import classA from '../../util/a';
import classB from '../../util/b';
import classC from '../../util/c';

let engligh = {
   teacher: 'english', age: 47
};

classA.push(engligh);
classB.push(engligh);
classC.push(engligh);